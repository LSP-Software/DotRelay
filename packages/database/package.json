{
  "name": "@dotrelay/database",
  "private": true,
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "format": "biome format . --write",
    "format:check": "biome format .",
    "lint": "biome lint .",
    "typecheck": "bun x prisma generate --config prisma.config.ts && tsc --project tsconfig.json",
    "test:unit": "bun test src",
    "test:integration": "bun test src/persistence/postgres.integration.test.ts src/persistence/trust.integration.test.ts",
    "db:validate": "bun ../../scripts/validate-prisma.ts",
    "db:migrate-check": "bun ../../scripts/check-migrations.ts"
  },
  "dependencies": {
    "@better-auth/prisma-adapter": "^1.7.1",
    "@prisma/adapter-pg": "^7.9.1",
    "@prisma/client": "^7.9.1"
  },
  "devDependencies": {
    "@electric-sql/pglite": "^0.5.5",
    "@types/pg": "^8.23.1",
    "pg": "^8.23.0",
    "prisma": "^7.9.1"
  }
}
