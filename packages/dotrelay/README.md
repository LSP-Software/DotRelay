# dotrelay npm selector

This package contains the private native-binary selector for the DotRelay standalone CLI. It
forwards the complete argument contract unchanged, contains no JavaScript secret client, and does
not accept portable credentials. The package is not currently published.
